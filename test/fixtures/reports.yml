# Read about fixtures at http://ar.rubyonrails.org/classes/Fixtures.html

joe_active_report:
  reporter_id:  <%= Fixtures.identify(:user) %>
  suspect_id:   <%= Fixtures.identify(:joe) %>
  reason:       Joe should really be reported.
  done:         false
  decision:     " "

joe_done_report:
  reporter_id:  <%= Fixtures.identify(:user) %>
  suspect_id:   <%= Fixtures.identify(:joe) %>
  reason:       "Over and over again!"
  done:         true
  decision:     "But we are all friends."
