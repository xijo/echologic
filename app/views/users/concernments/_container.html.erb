<div id="concernment_container" class="details_container">
  <% concernments = @user.concernments %>

  <% for sort in Concernment.sorts do %>
  
    <h2><%= sort[1] %></h2>
    <%= render :partial => 'users/concernments/list', :locals => { :concernments => concernments.find_all_by_sort(sort[0]), :sort => sort[0] } %><br/>
      
  <% end %>

</div>

<script type="text/javascript">
  $(".tag_value_autocomplete").autocomplete("users/concernments/auto_complete_for_tag_value");
</script>
