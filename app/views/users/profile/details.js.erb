/* Render details into the corresponding container. */

var content = "<%= escape_javascript(render(:partial => 'profile_details')) %>";

$('#profile_details_container').html(content);

$('#profile_details_container:hidden').show();
