<div id="loginContainer">

  <% if current_user_session %>
    <%= image_tag current_user.picture.url(:small) %>
    <span>
      Hello <%= current_user.prename %><br/>
      <%= link_to "Logout", user_session_path, :method => :delete %><br/>
      <%= link_to "Profile", profile_path %>
    </span>

  <% else %>
    <% form_for UserSession.new, :url => user_session_path do |f| %>
      <span class="label"><%= t('.email') %></span>
      <span class="label"><%= t('.password') %></span><br/>
      <%= f.text_field :email, :class => 'NEWloginField' %>
      <%= f.password_field :password, :class => 'NEWloginField' %>
      <%= f.submit 'Login', :class => 'ui-button ui-state-default ui-corner-all' %><br/>
      <%= link_to 'Signup', new_user_path %><br/>
      <%= link_to 'Forgot', new_password_reset_path %><br/>
      <span class="sublabel"><%= link_to(t('.remember'), '#') %></span>
      <span class="sublabel"><%= link_to(t('.forgot'), '#') %></span>
    <% end %>
  <% end %>
</div>