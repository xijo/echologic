<% form.fields_for :document do |doc|  %>
  <%= doc.label :title %>
  <%= doc.text_field :title %>
  <br />
  <%= doc.label :text %>
  <%= doc.text_area :text, :size => '40x5' %>
  <br />
<% end -%>
<%= form.hidden_field :parent_id %> 
<%= form.submit "Save", :id => 'save_button' %>
